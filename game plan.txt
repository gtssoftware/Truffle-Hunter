the dos application would run a file. the file is THE1.lump, lumps would be a special file, basically a renamed zip, containing all of the assets (ex : scipts, images, sounds), as a single file, and the exe would run the scripts from THE1.lump and there would be 4 episodes, so episode 1 is THE1.lump, but for now it will just be episode 1. the exe itself would be JUBBA.exe, but its not an installer.